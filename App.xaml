<Application x:Class="DeadDailyDose.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
             xmlns:local="clr-namespace:DeadDailyDose"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <controls:BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
            <local:RepeatModeConverter x:Key="RepeatModeConverter"/>
            <DataTemplate x:Key="RepeatModeItemTemplate">
                <TextBlock Text="{Binding Converter={StaticResource RepeatModeConverter}}" Foreground="{StaticResource BrushTextPrimary}"/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
